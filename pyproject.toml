# 中文：Python 项目元数据与依赖配置，定义运行依赖、开发依赖和测试配置。
# English: Python project metadata and dependency configuration for runtime, development, and tests.

[project]
name = "origami-devops-platform"
version = "0.1.0"
description = "Local-first mini PIC 2.0 DevOps platform scaffold"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "prometheus-client>=0.21",
  "pyyaml>=6.0",
  "uvicorn>=0.30",
]

[project.optional-dependencies]
dev = [
  "matplotlib>=3.9",
  "numpy>=2.0",
  "pytest>=8.3",
  "ruff>=0.6",
]
export = [
  "onnxruntime>=1.19",
  "torch>=2.4",
]
rl = [
  "gymnasium>=1.0",
  "stable-baselines3>=2.3",
  "pettingzoo>=1.24",
  "mapie>=0.9",
]
viz = [
  "matplotlib>=3.9",
  "numpy>=2.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
